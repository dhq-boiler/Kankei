<Window x:Class="Kankei.Desktop.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        AllowsTransparency="True" Background="Transparent" WindowStyle="None"
        ShowInTaskbar="False" ShowActivated="False" Topmost="True" SizeToContent="WidthAndHeight">
  <Border Background="#E51A1A1A" CornerRadius="16" Padding="28" MinWidth="460">
    <StackPanel>
      <TextBlock x:Name="TitleText" FontSize="22" FontWeight="SemiBold" Foreground="White" />
      <TextBlock x:Name="ProgressText" Margin="0,8,0,0" Foreground="#FFD0D0D0" />
      <ItemsControl x:Name="ItemsList" Margin="0,16,0,0">
        <ItemsControl.ItemTemplate>
          <DataTemplate><TextBlock Foreground="White" Margin="0,3" Text="{Binding}" /></DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </StackPanel>
  </Border>
</Window>
